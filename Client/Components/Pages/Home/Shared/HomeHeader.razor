@using Client.Components.Pages.Home.Shared.Skeletons

<HeaderContent Class="flex items-center gap-4 py-3 ps-3 pe-0 bg-background">

    <BbAvatar>
        <BbAvatarFallback Class="bg-primary text-primary-foreground">D</BbAvatarFallback>
    </BbAvatar>

    <CategorySelector Selected="@Selected" OnToggle="@OnToggle" />

</HeaderContent>

@code
{
    [Parameter] public HashSet<string> Selected { get; set; } = [];
    [Parameter] public EventCallback<string> OnToggle { get; set; }
}