<!-- Bottom Navigation -->
<nav class="fixed right-0 bottom-0 left-0 z-50 border-t border-white/10 bg-white/5 backdrop-blur-2xl supports-[backdrop-filter]:bg-white/5">

    <!-- Ambient Glow -->
    <div class="pointer-events-none absolute inset-0 bg-gradient-to-r from-indigo-500/0 via-indigo-500/10 to-pink-500/0">
    </div>

    <div class="relative mx-auto flex max-w-6xl items-center
                justify-around px-6 py-3">

        <!-- Home -->
        <NavLink href="/" Match="NavLinkMatch.All"
                 class="group flex flex-col items-center gap-1 text-xs text-slate-400 transition"
                 ActiveClass="text-indigo-400">

            <Blazicon Svg="Lucide.House" class="size-5! transition group-hover:scale-110" />
            <span>Home</span>
        </NavLink>

        <!-- Search -->
        <NavLink href="/search"
                 class="group flex flex-col items-center gap-1 text-xs text-slate-400 transition"
                 ActiveClass="text-indigo-400">

            <Blazicon Svg="Lucide.Search" class="size-5! transition group-hover:scale-110" />
            <span>Search</span>
        </NavLink>

        <!-- Trending -->
        <NavLink href="/trending"
                 class="group flex flex-col items-center gap-1 text-xs text-slate-400 transition"
                 ActiveClass="text-indigo-400">

            <Blazicon Svg="Lucide.TrendingUp" class="size-5! transition group-hover:scale-110" />
            <span>Trending</span>
        </NavLink>

        <!-- Favorites -->
        <NavLink href="/favorites"
                 class="group flex flex-col items-center gap-1 text-xs text-slate-400 transition"
                 ActiveClass="text-indigo-400">

            <Blazicon Svg="Lucide.Heart" class="size-5! transition group-hover:scale-110" />
            <span>Saved</span>
        </NavLink>

    </div>
</nav>